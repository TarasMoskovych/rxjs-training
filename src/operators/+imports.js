const { Observable } = rxjs;
const {
    take,
    pipe,
    map,
    pluck,
    first,
    last,
    find,
    findIndex,
    skip,
    skipWhile,
    skipUntil,
    takeUntil,
    filter,
    debounceTime,
    distinct,
    distinctUntilChanged,
    buffer,
    bufferTime,
    bufferCount,
    defaultIfEmpty,
    every,
    tap,
    delay,
    merge,
    mergeAll,
    concat,
    concatAll,
    mergeMap,
    concatMap,
    zip,
    withLatestFrom,
    catchError
} = rxjs.operators;






